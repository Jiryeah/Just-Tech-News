<ol class="post-list">
  {{#each posts as |post|}}
  <li>
    {{!-- to use a partial, use '>' followed by 
    the name of the partial file and the object 
    that we'll pass to the partial --}}
    {{> post-info post }}
  </li>
  {{/each}}
</ol>

{{!-- '#each' is the beginning of the loop  --}}
{{!-- '/each' is the end of the loop  --}}
{{!-- Any HTML code in between the (e.g., <li> element) will be repeated for every item in posts  --}}